interface:
  display_name: "Motion Icon Designer"
  short_description: "Design and build production semantic motion icons"
  default_prompt: "Use $design-motion-icons to analyze this product-state icon, design semantic motion, and when production output is requested, build and verify an SVG+WAAPI production package before calling it production-ready."
